//renderCategoriesSelect();

//function renderCategoriesSelect(categories) {
  //  option.innerHTML = 'categories';

//}
    // TODO.
    // 1. Для всех элементов класса: categoryes-select
    // 2. Сделать перерендер innerHtml <option(s)> на основе categoryes
    // 3. Вызывать функцию на старте, при всяком изменении массива categoryes
